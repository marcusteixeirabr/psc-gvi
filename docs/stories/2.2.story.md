# Story 2.2 — Dashboard Principal

## Status: Done

## Story

**Como** inspetor (qualquer role),  
**Quero** ver na página inicial os navios estrangeiros com port calls ativos,  
**Para que** possa identificar rapidamente quem está na janela de inspeção.

## Acceptance Criteria

- [x] AC1: GET / exibe o dashboard (substituiu o placeholder JSON)
- [x] AC2: Lista apenas navios com `category = 'estrangeiro'` e port call `planned` ou `active`
- [x] AC3: Linhas ordenadas por prioridade (P1 → P2 → P3 → N/A) e depois por ETA
- [x] AC4: Cada linha exibe: Prioridade, Nome/IMO, LOA×Boca, Terminal, ETA, ETD, Status
- [x] AC5: Linhas P1 destacadas em vermelho-claro, P2 em amarelo-claro
- [x] AC6: Cards de estatística: total de navios, na janela (P1+P2), aguardando CIALA
- [x] AC7: Botão "↻ Atualizar ZP-21" visível para admin e editor
- [x] AC8: Prioridade N/A (não P1) exibida quando não há dados do CIALA

## Tasks

- [x] T1: Query `ListDashboardEntries` em `port_call.sql` (JOIN vessels + port_calls)
- [x] T2: `internal/dashboard/handler.go` com conversão de tipos e ordenação por prioridade
- [x] T3: `web/templates/dashboard.html`
- [x] T4: Funções exportadas `CalcPriority` e `PriorityLabelFor` em `internal/vessel/service.go`
- [x] T5: Registrar `GET /` em `main.go` + função `sub` para templates
- [x] T6: Correção de flash (verde/vermelho) em `vessel_detail.html`

## File List

- `internal/db/query/port_call.sql` (modificado)
- `internal/db/sqlc/port_call.sql.go` (gerado)
- `internal/vessel/service.go` (modificado — exports)
- `internal/dashboard/handler.go` (novo)
- `web/templates/dashboard.html` (novo)
- `cmd/server/main.go` (modificado)
- `web/templates/vessel_detail.html` (modificado — flash)
- `internal/vessel/handler.go` (modificado — flash + merge/delete)

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6 (@dev / Dex)
